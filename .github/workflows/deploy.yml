name: Deploy Laravel

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check secrets
        run: |
          echo "HOST=${{ secrets.HOST }}"
          echo "USERNAME=${{ secrets.USERNAME }}"